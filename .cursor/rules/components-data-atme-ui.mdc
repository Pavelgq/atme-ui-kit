---
description: Использовать AtmeRoot для корня всех компонентов
globs: src/components/**/*.tsx
alwaysApply: false
---

# data-atme-ui через обёртку AtmeRoot

Корневой DOM-элемент каждого компонента библиотеки должен быть обёрнут в **AtmeRoot**. Компонент сам добавляет атрибут `data-atme-ui` и пробрасывает `className`, `testId` → `data-testid`. Это даёт изолированный сброс стилей (`box-sizing: border-box` и др.) только внутри компонентов.

## Как использовать

Импорт и использование на корневом элементе:

```tsx
import { AtmeRoot } from '@components/primitives/AtmeRoot';

// Обычный корень (div)
return (
  <AtmeRoot className={cn(styles.root, className)} testId={testId}>
    {children}
  </AtmeRoot>
);

// Корень — другой тег (button, a, span и т.д.)
return (
  <AtmeRoot as="button" className={cn(styles.button, className)} testId={testId} type="button" {...props}>
    {children}
  </AtmeRoot>
);
```

Если нужно пробросить ref и все пропсы на корень (polymorphic), передавайте их в AtmeRoot:

```tsx
<AtmeRoot ref={ref} as={as} className={cn(styles.tag, className)} testId={testId} {...props} />
```

## Где применяется

- Primitives: Button, Input, Badge, Tag, Icon, Typography и т.д.
- Layout: Grid, Stack, Island, WindowFrame
- Content: ArticlePreview
- Navigation: SideMenu, SideMenuItem

Правило сброса в `src/styles/scoped-reset.css`:

```css
[data-atme-ui],
[data-atme-ui] *,
[data-atme-ui] *::before,
[data-atme-ui] *::after {
  box-sizing: border-box;
}
```
